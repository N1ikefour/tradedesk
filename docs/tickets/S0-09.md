# S0-09 — SETUP.md, черновик

**Статус:** Review
**Роль:** infra
**Ветка:** `docs/S0-09-setup`
**Источник:** `SPEC.md` §12, этап 0

## Зачем

Последняя задача этапа 0. После неё человек должен уметь развернуть журнал у себя и войти.

Из `SPEC.md` §12: «установка Docker Desktop, клонирование, `make init`, `start`, вход. DoD: сосед по проекту прошёл по нему без вопросов».

**Это черновик для разработчика, а не финальная инструкция.** Финальная — `T-01`, она для не-разработчика на Windows, со скриншотами, разделом «если что-то не так» и прогоном на чистой Windows-VM. Не делай её работу здесь: без скриншотов, без установки коллектора, без `install-service.ps1`.

## Читатель

Человек, который умеет открыть терминал и установить программу, но **этого репозитория не знает**. Не знает, что такое `make`, `worktree`, `MASTER_KEY`, профиль compose. Не должен читать `SPEC.md`, `CLAUDE.md` или `docs/DEVELOPMENT.md`, чтобы дойти до входа в приложение.

Целевая машина — **Windows, 16 ГБ**. Разработка идёт на macOS: значит windows-путь описывается, но **проверить его здесь нельзя** — см. «Честность» ниже.

## Что должно быть в SETUP.md

Последовательность до первого успешного входа:

1. **Предусловия.** Docker Desktop (для Windows — с WSL2), Git. Версии, которые реально требуются, а не «поновее». Сколько места и памяти нужно.
2. **Клонирование.**
3. **`make init`** — что он делает и почему `.env` нельзя потерять: там `MASTER_KEY`, которым зашифрованы пароли счетов. Потеря = потеря паролей.
4. **`make up`** — что поднимается, сколько это занимает в первый раз.
5. **Вход.** Кода в письме нет, почта не подключена: код лежит на странице `/api/v1/dev/outbox`. Сказать это прямо — иначе человек будет ждать письмо.
6. **Что уже работает, а чего ещё нет.** Дашборд, журнал, календарь, счета — заглушки. Работают вход и настройки. Не создавать ложных ожиданий.
7. **`make down`** и что данные переживают остановку.

Отдельно, коротко: **что делать, если порт занят** — у нас самих 5432 и 6379 были заняты чужим проектом, лечится переменными в `.env`. Это единственный сценарий «если что-то не так», который мы наблюдали лично; остальные не выдумывай — они в `T-01` после реального прогона.

## Отдельно: `CLAUDE.md` §4 устарел

Раздел «Заглушки» перечисляет `make init / up / down` (S0-06), `make migrate / revision` (S0-02, S0-03), `make types` (S0-07). **Все они давно работают** — проверь по `make help` и убери раздел.

Этот файл читает каждый агент перед каждой задачей, поэтому неверные сведения в нём стоят дороже, чем где-либо ещё. Заодно сверь остальной §4 с фактическим `make help`: там появились цели, которых в списке нет.

## Честность — главное требование задачи

DoD в `SPEC.md` — «сосед прошёл без вопросов». Соседа у нас нет, Windows-машины тоже. Значит:

- **Что можно проверить здесь — проверь реально.** Пройди по своей же инструкции на macOS от `make init` до входа. Не «прочитал и выглядит верно», а выполнил.
- **Чего проверить нельзя — назови в тексте инструкции явно.** Windows + WSL2, `start.bat`, поведение Docker Desktop на Windows. Пометка в самом `SETUP.md`, чтобы читатель знал, где инструкция опирается на предположение.
- В итог тикета — отдельным разделом, что прогонялось и на чём.

**Чистая проверка без разрушения дев-данных.** В томе `td_pgdata` лежат локальные пользователи, которыми мы проверяем экраны; в `.env` рабочих worktree — `MASTER_KEY`, которым зашифрованы данные в этом томе. Не удаляй том и не перезаписывай чужие `.env`.

Для честного прогона «с нуля» подними изолированный экземпляр: свой `COMPOSE_PROJECT_NAME` (например `tradedesk-setuptest`) даёт отдельные тома и сеть. За собой убери — и контейнеры, и созданные тома.

## Границы

- Только `SETUP.md` (новый, в корне) и `CLAUDE.md` §4.
- `docs/DEVELOPMENT.md` не дублировать: он про разработку, `SETUP.md` — про запуск. Где уместно — сослаться.
- Коллектор MT5 не описывать: он ставится в `T-01`, отдельной задачей и на Windows.
- Прод не описывать: для этого есть `docs/PRODUCTION_DEPLOY.md`.

## Acceptance criteria

- Человек, не читавший других файлов проекта, доходит по `SETUP.md` до входа в приложение.
- Ни один шаг не требует знания `make`, compose-профилей или устройства репозитория.
- Про `MASTER_KEY` сказано так, что его не потеряют.
- Про код входа на `/api/v1/dev/outbox` сказано до того, как человек начнёт ждать письмо.
- Явно перечислено, что ещё не работает.
- Места, не проверенные на Windows, помечены в тексте.
- `CLAUDE.md` §4 соответствует `make help`.
- `make ci` зелёный.

## Зависимости

`S0-06` (compose), `S0-08` (настройки) — влиты. Закрывает этап 0.

## Связанные

`T-01` — финальная инструкция для не-разработчика, после прогона на чистой Windows-VM.
`X-12` — том postgres переживает пересоздание worktree; причина, по которой нельзя просто удалить том.

---

## Итог

Сделано. Ветка `docs/S0-09-setup`. Закрывает этап 0.

### Что получилось

`SETUP.md` — 333 строки, доводят человека от пустой машины до входа в приложение без чтения других файлов проекта.

Прогнан **целиком, реальным выполнением**, а не вычиткой: создание `.env`, отказ повторного `make init` перезаписывать существующий, запуск с занятым портом и его исправление через `.env`, подъём всех четырёх контейнеров, вход по коду со страницы писем, остановка и повторный запуск с сохранением данных, запуск скриптами напрямую без `make`. На изолированном compose-проекте — дев-данные и том `td_pgdata` не пострадали.

Два поведения найдены прогоном и вписаны в текст: уход на страницу писем **сбрасывает форму входа** (обойдено второй вкладкой, заведён `X-16`), и поле кода **отправляется само** по шести цифрам.

### Задача оказалась пробником по документации

`SETUP.md` прошёл по документам насквозь и показал, что почти все они описывают состояние, которого давно нет:

| Документ | Что утверждал | Факт |
|---|---|---|
| `CLAUDE.md` §4 | шесть целей — заглушки, падают с ненулевым кодом | все работают |
| `CLAUDE.md` §4 | коллектор запускается `run-collector.bat` | файла нет, появится в `S1-10` |
| `CLAUDE.md` §6 | testcontainers поднимают Postgres и minio | Postgres и Redis |
| `CLAUDE.md` §6 | порог покрытия 90 % | `pytest-cov` не в зависимостях |
| `README.md` | «Этап 0, кода ещё нет» | семь задач влито |
| `README.md` | `SETUP.md` — инструкция по установке коллектора | инструкция по запуску приложения |
| `docs/DEVELOPMENT.md` | «окружение ещё не собрано» | собрано |
| `docs/PROJECT_CONTEXT.md` | «продуктового кода нет, приложения пустые» | 8 доменов, 10 эндпоинтов, 40+ файлов фронта |
| `docs/ARCHITECTURE.md` | схема — 13 таблиц одной ревизией | 14 таблиц двумя |
| `docker-compose.yml` | тесты поднимают minio через testcontainers | не поднимают |

Скоуп задачи расширялся дважды, оба раза осознанно и моим решением. Причина: `SETUP.md` ссылается на `DEVELOPMENT.md`, тот на `PROJECT_CONTEXT.md`, и инструкция приводила бы читателя к тексту «проекта пока нет». Оставить это значило обесценить саму работу.

### Системная правка

Расхождение накопилось не по невнимательности. **Документы писались авансом, под будущее состояние, а ревью диффа их не видит**: файла нет в диффе — значит его не смотрят.

В DoD (`CLAUDE.md` §7) добавлен пункт: задача, изменившая состояние проекта, правит соответствующий документ **в том же PR**. С объяснением причины прямо в тексте, чтобы пункт не выкинули как бюрократию.

Оба переписанных документа получили правило против повторного протухания: статусы задач живут только в `BOARD.md` и не дублируются; строка в таблице `ARCHITECTURE.md` появляется, когда код влит в `main`. Колонка «Состояние» из неё удалена — она и была источником расхождения.

### Проверено

- `make ci` зелёный, `exit=0`: 432 pytest, 91 vitest, сборка web.
- Все 21 упоминание `make`-цели сверены с `make help`; 27 путей проверены на существование; команды из «быстрых циклов» реально запущены.
- Полный холодный проход по `SETUP.md` на macOS (Docker 29.6.1, Compose v5.3.0).

### Не проверено — сказано и в тексте инструкции

- **Всё, что про Windows:** Docker Desktop с WSL2, Git Bash, `start.bat`/`stop.bat`. Windows-машины нет. Помечено плашкой в каждом таком месте плюс отдельным разделом «что здесь проверено, а что нет».
- **Время первого запуска на чистой машине.** Наши 40 секунд получены при скачанных образах и прогретом кэше. Придумывать число не стали.
- **Сохранение настроек:** экран открывали, значения не меняли.
- Рендер обоих переписанных документов на GitHub.

DoD из `SPEC.md` — «сосед прошёл по инструкции без вопросов» — **выполнить нельзя**: соседа и Windows-машины нет. Настоящая проверка это `T-01` с прогоном на чистой Windows-VM.

### Заведено по ходу

`X-16` — сброс формы входа. `X-17` — на Windows нет `init.bat`, первый шаг выполним только из Git Bash.
