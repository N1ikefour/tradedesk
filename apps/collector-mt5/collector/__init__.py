"""Коллектор MT5: забор истории сделок из терминала в TradeDesk. Только Windows.

Модули делятся на два сорта, и деление это принципиальное, а не косметическое.

**Проверяемые на любой машине** — ни одного вызова терминала: `messages`, `payload`,
`sync`, `state`, `config`, `logging_setup`, разбор ответов в `api_client` и цикл счёта
в `worker` (терминал приходит в него протоколом).

**Непроверяемый нигде, кроме Windows** — `mt5_client`: обёртка над библиотекой
`MetaTrader5`, которой на macOS и Linux не существует. Решений в ней нет намеренно.
"""
