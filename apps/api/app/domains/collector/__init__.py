"""Канал управления коллектором: heartbeat и выдача заданий (SPEC.md 5.3, 5.6).

Домен отделён от `domains/ingest` по владельцу маршрута, а не по префиксу пути.
`POST /ingest/deals` — канал данных: сделки, нормализация, пересборка позиций.
`POST /ingest/heartbeat` и `GET /internal/collector/assignments` — канал управления:
кто жив, за какими счетами следить, чем войти в терминал. У них общая авторизация по
сервисному токену и общее правило видимости счетов, и держать их в разных модулях
значило бы завести две копии обоих.
"""
