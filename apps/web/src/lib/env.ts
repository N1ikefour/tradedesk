/**
 * Признаки окружения, от которых зависит состав интерфейса.
 *
 * `/api/v1/dev/outbox` существует только при `APP_ENV=local` (app/main.py), в проде это
 * обычный 404. Профили compose совпадают с режимами сборки один в один: local поднимает
 * vite dev-сервер (`import.meta.env.DEV === true`), prod отдаёт собранную статику
 * (`false`). Поэтому отдельной переменной окружения для фронта не заводится: лишняя
 * настройка означала бы, что ссылку на страницу писем можно случайно оставить в проде.
 */
export const DEV_OUTBOX_AVAILABLE: boolean = import.meta.env.DEV;
